## 目标

一个网页版文件管理器.

## 功能

允许用户查看
- 目录下有哪些文件/目录 (包含 `.` 开头的).  无需显示 `./`, 当前目录的名字反映在 URL 中.
- 文件/目录的元信息 (文件名, 大小, 修改时间)
- 文件内容 (如果格式支持, 直接在浏览器中打开, 否则下载.  这应该无需特殊处理, 客户端浏览器会自己决定.)

用户可以在各个目录层级间导航.

允许用户上传本地文件到页面上所处的那个目录中.

在 PC 端, 允许用户上传整个文件夹.
上传文件夹使用 merge 的方案: 如果服务端存在同名文件夹, 则把用户上传的文件夹内的项目保存到那个同名目录下.
整个过程递归进行.

无需防覆盖, 且上传文件可以覆盖同名目录, 上传目录可以覆盖同名文件.

## 隐私性

每次启动自动使用 HTTPS 自签.

## 安全性

无需考虑, 保持代码简单.

## UI

实现功能即可, 保持代码简单和风格简约, 无任何说明文字 (除了上传区域以外, 上传区域显示 `上传文件`).

### 整体布局

整个页面就是一个列表, 竖排显示, 像抽屉一样, 但没有分割线.
从上至下:
- 第一格: `..`, 无需显示元信息
- 若干个目录格子, 有 `/` 后缀
- 若干个文件格子
- 上传格子
- diff 格子 (默认不显示)

### diff 格子

告知用户, 如果暂存区的文件落盘, 会对现有文件造成什么影响.

格式:

```
以下文件会被覆盖:
  - path/to/file1
  - path/to/file2
```

### 上传格子

自身就是一个按钮, 点击弹出文件 selector.
在 PC 端, 也可直接 drag&drop 文件到该格子.

后端首先暂存用户上传的所有项目.
如果单纯是新增, 则立即落盘; 否则, 新建一个 diff 格子, 里面包含多行.

点击 diff 格子右上角的 `✅` button 后, 才真正落盘, 然后刷新当前页面.

### 格式

```
[文件名]    [大小G/M/K/B]    [年]年[月]月[日]日 [时]:[分分]:[秒秒] [A/P]M
```

其中,
- 文件名左对齐.  (`..` 当成文件名处理)
- 大小舍入到一位小数, 若精确等于则使用整数.  对于目录, 什么都不显示.
- 月份/日期/hour 是整数, 右对齐, 不补零

整个列表的每一列都左对齐.

### 顺序

按照字典序.

### 可读性

单个格子行, 用淡色背景标注.
保证相邻行的背景色不一样.
只有上传格子使用白色, 只有 diff 格子使用浅灰色.

单个格子的四周到其文本有一定距离.

## 运行

以下参数从命令行读取:
- `-p PORT`: TCP 端口号
- `-d PUBLIC_DIRECTORY`: 要发布的目录
- `-s HTTPS_DIRECTORY`: 可选, 若提供, 使用那个目录下的证书; 否则在临时目录下创建随机目录, 以存放临时生成的证书文件

还需包含帮助信息.

## 技术栈

不论使用什么语言, 总之 0 第三方依赖, 只使用标准库.

允许调用本机的命令行程序生成证书.
